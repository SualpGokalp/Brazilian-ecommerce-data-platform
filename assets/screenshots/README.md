# Screenshots

Save the project screenshots here with **exactly** these file names so the
links in the main `README.md` render correctly on GitHub:

| File name | What it shows |
|-----------|---------------|
| `dashboard-overview.png` | Streamlit dashboard top: KPI metrics + monthly trend + order-status pie |
| `dashboard-categories-states.png` | Top selling categories + revenue by state |
| `dashboard-delivery-value.png` | Avg delivery time by state + order value distribution |
| `airflow-dag.png` | Airflow `dbt_pipeline` DAG runs (ingestion → run → test → docs) |
| `api-swagger.png` | FastAPI Swagger UI listing all endpoints |

PNG is preferred. Keep the images reasonably sized (< ~500 KB each) so the
repo stays light.
